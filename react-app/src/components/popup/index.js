// import React from "react";
// import Popup from 'reactjs-popup'

// const TestPopup = () => {
//     const [openPhotoError, setPhotoError] = useState(false)
//     const [openCommentError, setCommentError] = useState(false)

    // const photoError = (
    //     <Popup open={openPhotoError} closeOnDocumentClick>
    //             {errors.map(error, idx) => {
    //                 <li key={idx}>{error}</li>
    //             }}
    //     </Popup>
    // )
//     return (
//         // {errors.length && (
//         //     )}

//     )
// }

// export default TestPopup

//<Popup trigger={<button className="button">Add Photo</button>} modal>
{/* <PhotoForm />
</Popup> */}
